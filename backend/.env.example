DATABASE_URL="postgresql://user:password@localhost:5432/entidades_digitales?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="7d"
PORT=3000
# Comma-separated list of allowed origins for the frontend, e.g. "https://app.example.com".
# Defaults to "*" (allow any origin) — tighten this in production.
CORS_ORIGIN="*"
